apiVersion: v1
kind: Service
metadata:
  name: ms-post
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: "/health"
spec:
  selector:
    app: ms-post
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      name: http

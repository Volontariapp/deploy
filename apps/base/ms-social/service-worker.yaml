apiVersion: v1
kind: Service
metadata:
  name: worker-social
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: "/health"
spec:
  selector:
    app: worker-social
  ports:
    - protocol: TCP
      port: 4105
      targetPort: 4105
      name: http

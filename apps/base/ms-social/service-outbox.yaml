apiVersion: v1
kind: Service
metadata:
  name: outbox-social
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: "/health"
spec:
  selector:
    app: outbox-social
  ports:
    - protocol: TCP
      port: 4102
      targetPort: 4102
      name: http

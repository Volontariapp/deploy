apiVersion: v1
kind: Service
metadata:
  name: outbox-user
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: "/health"
spec:
  selector:
    app: outbox-user
  ports:
    - protocol: TCP
      port: 4101
      targetPort: 4101
      name: http

neo4j:
  name: neo4j
  acceptLicenseAgreement: "yes"
  password: "REPLACED_BY_KUSTOMIZE_PATCH"
  edition: community
volumes:
  data:
    mode: defaultStorageClass

services:
  default:
    enabled: true
    type: ClusterIP
  neo4j:
    enabled: true
    type: ClusterIP
